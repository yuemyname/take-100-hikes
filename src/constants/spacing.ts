/** 4pt spacing rhythm. Screen horizontal padding is `spacing.xl` (20). */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  huge: 48,
} as const;

/** Minimum touch target — 100PEAKS_MASTER_SPEC.md §17. */
export const MIN_TOUCH_TARGET = 44;
