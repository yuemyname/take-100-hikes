# 100PEAKS Codex Handoff

> 작성 기준: `main`의 `7f956bec04a7c122cd2c2e323db916fe36a1f3f5` (2026-09-08)
>
> 이 문서는 다음 Codex 작업자가 현재 상태를 다시 추측하지 않고 바로 이어서 작업하기 위한 실행용 인수인계다. 제품 규칙이 충돌하면 아래의 **문서 우선순위**를 따른다.

## 1. 지금 제품이 향하는 곳

100PEAKS(백픽스)는 한국의 명산 챌린지를 친구와 함께 채우는 **소셜 산 수집·정상 인증 앱**이다. 핵심 경험은 진지한 등산 도구나 경로 추적이 아니라 다음 네 가지다.

1. 산 정상에서 앱 카메라와 GPS로 인증한다.
2. 맞팔 친구와 하나의 실제 공동 인증 세션을 공유한다.
3. 인증한 산과 사진을 추억처럼 모은다.
4. 서로 다른 100대 명산 컬렉션의 진행률을 한 번의 등산 기록으로 함께 채운다.

수집 대상은 **산 인증 기록·스탬프·챌린지 진행률**이다. 캐릭터는 반복 등장하는 브랜드 배우이며, 산마다 새 캐릭터를 해금하는 시스템이 아니다.

제품명은 `100PEAKS`, 기본 UI 언어는 한국어다. 하단 탭은 정확히 `홈 / 명산 / 인증 / 친구 / MY` 다섯 개를 유지한다.

## 2. 반드시 먼저 읽을 파일

작업을 시작하기 전에 다음 순서대로 읽는다. 일부 오래된 설명이 남아 있으므로 한 파일만 보고 결정하지 않는다.

1. `AGENTS.md` — 모든 코딩 에이전트의 현재 작업 규칙
2. `100PEAKS_MASTER_SPEC.md` — 제품 행동, 내비게이션, 소셜·공동 인증의 기본 기준; 처음부터 끝까지 읽을 것
3. `docs/OFFICIAL_VISUAL_DIRECTION.md` — 최신 캐릭터·수집 대상·비주얼 해석; 캐릭터 관련 오래된 마스터 스펙 문구보다 우선
4. `docs/COLLECTIONS.md` — 최신 다중 컬렉션 모델; 단일 100대 명산을 가정하는 오래된 문구보다 우선
5. `docs/data/BAC100_DATA_POLICY.md`
6. `docs/data/bac100-candidates.csv` — BAC 후보 100개, 인증지명, 산림청 대응 관계, 좌표 검증 상태
7. `supabase/migrations/0001_init.sql`부터 최신 마이그레이션까지 순서대로
8. `src/features/collections/index.ts`, `src/types/database.ts`, `src/features/certification/`, `src/features/mountains/`
9. UI 작업이면 `docs/references/README.md`와 `docs/references/`의 이미지 3개를 실제로 확인하고, `assets/official/` 및 `src/data/officialArt.ts`도 확인한다.

`README.md`는 실행 방법과 큰 구조를 파악하는 보조 문서로만 사용한다. 현재 캐릭터 체계, 마이그레이션 목록, “다음 단계” 설명 일부는 최신 `AGENTS.md` 및 위의 두 결정 문서보다 오래되었다.

### 문서 우선순위

- 제품 행동·내비게이션·소셜·공동 인증: `100PEAKS_MASTER_SPEC.md`
- 최신 캐릭터·수집 대상·비주얼: `docs/OFFICIAL_VISUAL_DIRECTION.md`
- 최신 명산 목록·진행률·인증지 구조: `docs/COLLECTIONS.md`
- 에이전트 실행 규칙: `AGENTS.md`
- BAC 데이터 안전 기준: `docs/data/BAC100_DATA_POLICY.md`

## 3. 완료된 기반 작업

### Phase 1–5

- Expo + React Native + strict TypeScript + Expo Router 기반이 구성되어 있다.
- Supabase Auth/DB/Storage 연결과 `.env` 미설정 시 게스트 데모 모드가 있다.
- 디자인 토큰과 공용 UI 컴포넌트, 정확히 5개의 하단 탭이 있다.
- 산림청 중심의 기존 산 100개 데이터, 명산 도감, 검색·필터, 산 상세, 즐겨찾기, 홈·MY 진행률이 구현되어 있다.
- 팔로우/언팔로우, 맞팔 계산, 친구 검색, 프로필, 산별 인증자 목록이 구현되어 있다.
- 산 상세의 인증자 목록은 맞팔 친구를 먼저 분리하고 각 그룹 안에서 최신 인증 순으로 표시한다.
- GPS 반경 확인, 앱 카메라 촬영, 인증 사진 업로드, 인증 세션 생성, 생성자 확정이 구현되어 있다.
- 공동 인증은 한 `certification_session`과 여러 `certification_members`로 구현되어 있다. 초대 대상은 맞팔 친구뿐이며, 초대 수락자도 정상 반경을 통과해야 한다.
- 공동 인증 초대, 수락·거절, 24시간 만료, 참여 상태 및 완료 화면이 구현되어 있다.
- 로딩·빈 상태·오류 상태와 접근성 레이블이 여러 화면에 들어가 있고 일부 햅틱도 들어가 있으나, Phase 6 전체 완료로 간주하지 않는다.

### 공식 비주얼 체계 반영

- 홈, 산 상세, 인증, 친구, 공동 인증, MY의 주요 화면이 공식 캐릭터·스티커를 사용하는 방향으로 정리되었다.
- 산 카드의 중심 수집 대상은 캐릭터가 아니라 산과 완료 상태로 바뀌었다.
- 공식 에셋 레지스트리는 `src/data/officialArt.ts`다.
- `assets/mascots/`의 기존 실험용 에셋과 `Mascot` 컴포넌트는 아직 저장소에 남아 있다. 보존 대상이며 공식 4인 캐스트로 오해하지 않는다.

### 두 컬렉션 UI 및 스키마 기반

- 컬렉션 ID는 `forest_service_100`, `bac_100` 두 개로 고정되어 있다.
- `0005_collections.sql`이 `collections`, `collection_mountains`, `verification_points`, `profiles.primary_collection_id`를 추가한다.
- `0006_seed_forest_collection.sql`이 기존 산 100개를 산림청 컬렉션 멤버십으로 연결한다.
- 앱 타입에 `Collection`, `CollectionMountain`, `VerificationPoint`, `CollectionId`가 추가되어 있다.
- 첫 방문 시 대표 컬렉션 선택 안내가 나오며, 선택값은 현재 AsyncStorage의 `100peaks.primaryCollection`에 저장된다.
- 홈과 명산 도감에서 대표 컬렉션을 전환할 수 있다.
- 홈은 선택한 컬렉션 기준 인증 산 수와 `🏔️`, 추천 산, 남은 산 수를 보여준다.
- 명산 도감은 선택한 컬렉션으로 목록·검색·필터·완료 수를 바꾼다.
- MY는 대표 컬렉션을 크게 표시하고 산림청/BAC 진행률을 동시에 표시한다.
- 컬렉션 전환은 인증 레코드를 수정·삭제·복제하지 않는다.

### 2026-09-09 데이터·원격 DB 반영

- BAC 산 정체성 감사와 BAC 전용 대상 반영 후, 산림청 목록에서 빠져 있던 홍도 `깃대봉`을 공식 산림청 자료로 확인해 추가했다.
- 현재 산 정체성은 합계 121개이며 두 컬렉션은 각각 100개, 교집합 79개, 각 차집합 21개다.
- `0008_complete_collection_memberships.sql`과 `supabase/seed.sql`이 명시적 slug로 200개 멤버십을 구성하고 서버에서 최종 개수를 검증한다.
- 연결된 `100hikes` Supabase 프로젝트에 `0001`–`0008`과 seed를 적용했다. 적용 직후 원격 통계는 `mountains=121`, `collections=2`, `collection_mountains=200`이다.
- BAC 100개 인증지 좌표는 여전히 검증되지 않았다. `0013_seed_bac_verification_points.sql`이 BAC 인증지명 100개를 `verification_points`에 `pending`으로 만들고 모든 BAC 멤버십에 연결했으며, 좌표와 반경은 전부 `null`이다. BAC 전용 21개 및 깃대봉의 레거시 GPS 필드도 `null`이다.
- 연결된 원격 Supabase에 `0013`까지 적용됐다. 마이그레이션 내부 검증에서 BAC 인증지 소스 100개, 멤버십 정체성 매칭 100개, 연결 100개, distinct 인증지 100개, GPS 값이 들어간 pending 인증지 0개를 확인했다.

### 2026-09-09 인증 사진·프로필 편집 반영

- 사용자의 산별 최신 확정 인증 세션 `photo_url`이 명산 도감 썸네일, 산 상세 히어로, MY 인증 기록 사진에 표시된다. 전역 `mountains.image_url`은 변경하지 않는다.
- MY의 `프로필 · 홈 배경 수정`에서 아이디, 표시 이름, 소개, 프로필 사진, 개인 홈 산 배경을 수정할 수 있다. 홈 배경 미리보기는 현재 기기의 실제 홈 히어로 비율과 같은 중앙 크롭을 사용한다.
- `0010_profile_media.sql`이 `profiles.home_background_url`과 공개 읽기/소유자 전용 쓰기 정책을 가진 `profile-media` Storage 버킷을 추가한다.
- 이미지 선택은 Expo SDK 호환 `expo-image-picker`를 사용한다.
- 홈 상단 진행 표시는 `X / 100` 대신 `X 🏔️`이며, 프로필 사진과 개인 홈 배경을 즉시 반영한다.
- `0009_backfill_auth_profiles.sql`과 `0010_profile_media.sql`은 연결된 원격 Supabase에 적용됐고, 최종 dry-run에서 원격 DB가 최신 상태임을 확인했다.
- 로컬 개발 빌드와 `testflight` EAS 프로필의 인증 촬영 및 공동 인증 참여 화면에는 정상 위치 보정 토글이 있다. 저장된 검증 좌표가 있는 산만 사용하고, 실제 카메라 촬영은 계속 필요하다. 일반 `production` 프로필에서는 숨긴다.

### 2026-09-10 팔로우·맞팔 알림 반영

- `0011_social_notifications.sql`이 사용자별 인앱 알림 기록과 Expo 푸시 토큰을 추가한다. 새 팔로우가 생성되면 DB 트리거가 `new_follower` 또는 `mutual_follow` 알림을 한 번 생성한다.
- `0012_certification_invite_notifications.sql`이 실제 공동 인증 초대마다 `certification_invite` 알림을 한 번 생성한다. 적용 시점에 응답 가능한 기존 초대도 중복 없이 소급 반영한다.
- 알림과 기기 토큰은 소유자 전용 RLS로 보호한다. 토큰 등록 RPC는 항상 현재 `auth.uid()`에 바인딩하며, 로그아웃할 때 현재 기기의 토큰을 제거한다.
- 홈 헤더의 종 아이콘에서 알림 목록, 읽지 않은 개수, 모두 읽기, iPhone 알림 권한 설정을 확인할 수 있다. 팔로우 알림은 상대 프로필로, 공동 인증 알림은 해당 참여 화면으로 이동한다.
- 연결된 원격 Supabase에 `0011`이 적용됐고, EAS의 `com.hundredpeaks.app`에 Apple Push Notifications 키가 연결됐다.
- Expo Push Service의 전송 영수증 확인 및 영구적으로 만료된 토큰 자동 정리는 아직 후속 운영 작업이다.

## 4. 두 컬렉션의 불변 아키텍처

```text
mountains
  └─ 산 자체의 정체성

collections
  ├─ forest_service_100
  └─ bac_100

collection_mountains
  ├─ 컬렉션별 산 멤버십
  ├─ 컬렉션별 표시 순서
  └─ 해당 컬렉션에서 요구하는 verification_point 연결

verification_points
  ├─ 백운대 / 신선대 / 대청봉 / 관음봉 같은 실제 인증 지점
  ├─ 좌표와 반경
  └─ pending | verified | retired

certification_sessions + certification_members
  └─ 실제 등산·참여자 인증 기록; 특정 컬렉션에 종속시키지 않음
```

반드시 지킬 계산 규칙:

- 사용자 완료 산은 `certification_members.status = 'confirmed'`에서 나온 distinct mountain ID다.
- 한 산을 여러 번 올라도 한 컬렉션 진행률에는 한 번만 센다.
- 한 번의 정상 인증이 여러 컬렉션의 산·인증지 조건을 충족하면 모든 해당 컬렉션에 반영한다.
- 컬렉션별로 별도의 인증 세션을 복제하지 않는다.
- 대표 컬렉션은 홈과 도감의 기본 보기일 뿐, 인증 기록의 소유 컬렉션이 아니다.
- 같은 이름의 산이나 같은 산의 다른 봉우리를 문자열만으로 합치지 않는다. 산 정체성과 인증 지점을 별도로 확인한다.

예시:

```text
설악산 정상 인증 1건
  ├─ 산림청 100대 명산 멤버십 조건 충족 → +1
  └─ BAC 명산100 멤버십 조건 충족       → +1
```

## 5. 현재 데이터 상태 — “UI 연결”과 “운영 데이터 완료”를 구분할 것

### 산림청 100

- `src/data/mountains.json`과 `supabase/seed.sql`에는 기존 산림청 중심 산 100개가 있다.
- `0006_seed_forest_collection.sql`이 이 100개를 `forest_service_100`에 연결한다.
- 현재 인증 클라이언트와 DB 트리거는 여전히 `mountains.latitude`, `mountains.longitude`, `mountains.verification_radius_m`를 사용한다.

### BAC 100

- `docs/data/bac100-candidates.csv`에는 BAC 후보 100개가 순서, 지역, 산 이름, 인증지명과 함께 정리되어 있다.
- CSV의 100개 좌표 상태는 모두 `pending`이다. 이 파일에는 인증용 위도·경도 자체가 들어 있지 않다.
- 산림청 데이터와 대응 가능한 대상은 79개다.
- BAC에만 있는 대상은 21개이고, 산림청에만 있는 대상도 21개다.
- 클라이언트의 `src/features/collections/index.ts`에는 BAC 100 이름 집합이 있고, 기존 산림청 데이터와 이름을 맞춰 79개를 임시로 BAC 도감에 보여준다.
- `BAC_CONNECTED_COUNT = 100`, `BAC_PENDING_COUNT = 21`이 현재 UI 안내에 사용된다.
- DB의 `bac_100` `collection_mountains` 멤버십은 명시적 slug 매핑으로 100개가 시드되어 있다.
- DB의 BAC `verification_points` 100개는 인증지명만 시드되어 각 `bac_100` 멤버십에 연결됐다. 모두 `coordinate_status='pending'`이고 위도·경도·반경은 `null`이다.
- BAC 전용 21개 도전 대상은 정식 산 정체성으로 연결되어 있다. 기존 오서산 정체성은 재사용하고 나머지 BAC 전용 산을 추가했다.
- 따라서 BAC 목록 100개와 DB 멤버십은 완료됐지만, GPS 인증은 인증지 시드·좌표 검증 전까지 완료된 것이 아니다.

### BAC 전용 21개

1. 수락산
2. 청계산
3. 연인산
4. 감악산(원주/제천)
5. 오대산 노인봉
6. 함백산
7. 청화산
8. 칠보산
9. 가야산(예산)
10. 광덕산
11. 오서산
12. 용봉산
13. 구봉산
14. 지리산 바래봉
15. 지리산 반야봉
16. 달마산
17. 덕룡산
18. 동악산
19. 불갑산
20. 축령산(장성)
21. 조령산

이름이 겹치거나 대표 봉우리가 다른 사례를 특히 주의한다: 감악산, 오대산 노인봉/비로봉, 가야산(예산/합천), 백운산, 축령산(장성/경기), 지리산 본봉/바래봉/반야봉 등.

### 아직 남은 구조적 연결

- `profiles.primary_collection_id`는 DB에 있으나 현재 앱 선택값은 로컬 AsyncStorage에만 저장된다.
- DB에는 최종 컬렉션 목록·순서가 있지만 클라이언트는 아직 임시 이름 매칭에 의존한다.
- 인증 화면, 초대 수락 화면, 서버 반경 트리거는 `verification_points`가 아닌 기존 `mountains` 좌표를 사용한다.
- 한 산이 컬렉션마다 다른 인증지를 요구할 때 어느 인증지로 시작하고 어떤 컬렉션에 인정할지 세션 계약을 명시하고 구현해야 한다. 필요한 경우 `certification_sessions.verification_point_id` 같은 최소 스키마 확장을 검토하되, 먼저 기존 세션 호환성과 진행률 파생 규칙을 설계한다.

## 6. 공식 캐릭터와 스티커 규칙

### 공식 캐릭터

- `assets/official/`에서 파일명에 `Take a Hike_Character`가 들어간 YELLOW, RED, BLUE, PINK 네 색상 패밀리가 공식 반복 캐스트다.
- 번호가 다른 파일은 새로운 캐릭터가 아니라 같은 네 캐릭터의 포즈·변형이다.
- 산별 캐릭터로 매핑하지 않는다. 100개의 캐릭터 수집 시스템을 만들지 않는다.
- 공식 래스터 아트를 SVG로 다시 그리거나, 체형을 바꾸거나, 재색칠하거나, 산별 대체 캐릭터를 생성하지 않는다.
- `expo-image`의 `contentFit="contain"`을 기본으로 원본 비율을 보존한다.
- `assets/mascots/`는 삭제하지 않는다. 향후 계절 캠페인, 이스터에그, 빈 상태, 이벤트 배지 등에 쓸 수 있는 지원·실험 라이브러리다.

### 공식 스티커

- 안정적인 ASCII 별칭은 `assets/official/stickers/`에 있고 코드 매핑은 `src/data/officialArt.ts`에 있다.
- 스티커는 화면 상태와 문맥에 맞춰 소량 사용한다. 예: `summit-check`는 인증, `did-it`/`summit-success`는 성공, `fresh-air`는 풍경·홈 히어로.
- 장식이 GPS 상태, 카메라, 안전 안내, 내비게이션, 텍스트 대비, 터치 영역을 가리면 안 된다.
- `save-me` 같은 농담 이미지를 실제 비상·안전 안내로 보이게 사용하지 않는다.

### 브랜드·법적 표현

- `BAC 명산100 기준`처럼 텍스트로 데이터 기준을 설명할 수 있다.
- BLACKYAK/BAC와 공식 제휴한 것처럼 표현하지 않는다.
- 허가 없이 BAC 로고, 브랜드 UI, 시각 아이덴티티를 복제하지 않는다.
- 레퍼런스의 Netflix 로고·타이포·타이틀 아트·캐릭터를 복제하지 않는다.

## 7. 절대 하지 말 것

- 하나의 고정 100대 명산 목록만 있는 제품으로 되돌리지 않는다.
- 컬렉션을 바꿀 때 인증 이력을 초기화·이동·복제하지 않는다.
- 동일 산이 두 컬렉션에 있다는 이유로 인증 세션을 두 개 만들지 않는다.
- BAC 최종 구현을 `BAC_NAMES` 문자열 필터로 끝내지 않는다.
- 미확인 좌표를 추측·평균·근삿값으로 넣거나 `verified`로 표시하지 않는다.
- `coordinate_status=pending` 또는 `retired`인 인증지를 실제 인증에 사용하지 않는다.
- `mountains`와 `verification_points`를 같은 개념으로 합치지 않는다.
- 동명이산·대표봉 차이를 이름 정규화만으로 자동 병합하지 않는다.
- 하단 탭을 추가·삭제하거나 순서를 임의로 바꾸지 않는다.
- 공동 인증을 업로드 후 친구 태그 기능으로 축소하지 않는다.
- 맞팔이 아닌 사용자를 공동 인증에 초대하지 않는다.
- 산 상세에서 일반 인증자를 맞팔 인증자보다 위에 섞지 않는다.
- 공식 4인 캐스트를 산별 수집 캐릭터로 바꾸지 않는다.
- `assets/mascots/` 또는 원본 공식 에셋을 정리한다는 이유로 삭제하지 않는다.
- 크림 배경과 강한 원색의 편집 디자인을 일반적인 초록색 피트니스 UI로 바꾸지 않는다.
- 서비스 역할 키나 다른 비밀 값을 클라이언트·커밋에 넣지 않는다.

## 8. 다음 구현 우선순위 — 이 순서대로 진행

기존 Phase 1–5를 다시 만들지 않는다. 현재 가장 먼저 막힌 것은 BAC 100의 안전한 데이터 완성 및 새 인증지 모델 연결이다.

2026-09-11 기준으로 1–4번은 완료됐다. 다음 구현은 5번부터 시작한다.

### 1) BAC 100 원본 목록과 산 정체성 잠금 — 완료

- `docs/data/bac100-candidates.csv`의 100개 순서, 산 이름, 지역, 인증지명을 현재 신뢰 가능한 BAC/지도/공공 자료와 대조한다.
- 79개 대응 관계와 BAC 전용 21개를 다시 확인한다.
- 동명이산과 여러 봉우리 사례를 문자열 정규화가 아니라 지역·대표봉·출처로 확정한다.
- 확인한 출처와 확인일을 데이터 또는 마이그레이션 주석에 남긴다.

### 2) BAC 전용 21개를 정식 `mountains` 데이터로 추가 — 완료

- 안정적인 UUID/slug, 한글명, 영문명, 고도, 지역, 설명, 표시용 메타데이터를 추가한다.
- 기존 산과 중복 레코드를 만들지 않는다. 지리산 바래봉/반야봉처럼 제품상 별도 도전 대상인 경우에는 그 이유를 명시한다.
- 현재 `mountains.latitude/longitude`가 `NOT NULL`이고 인증에도 재사용되는 기술 부채를 먼저 처리한다. 표시용 대표 좌표와 인증용 좌표를 혼동하지 말고, 검증되지 않은 인증 좌표로 제약을 억지로 채우지 않는다.
- `src/data/mountains.json`과 `supabase/seed.sql`의 생성·동기화 경로를 함께 정리한다.

### 3) 두 컬렉션의 DB 멤버십을 완성 — 완료

- 기존 79개는 같은 `mountain_id`를 재사용한다.
- BAC 전용 21개를 더해 `bac_100`의 `collection_mountains`가 정확히 100개가 되도록 새 idempotent 마이그레이션을 만든다.
- BAC 공식 순서 1–100을 `display_order`로 저장한다.
- 산림청 100 멤버십도 정확히 100개인지 검증하고 두 컬렉션의 교집합 79, 각 차집합 21을 자동 검사한다.

### 4) BAC 인증지 100개를 `verification_points`로 시드 — 완료

- CSV의 인증지명으로 산과 인증지 레코드를 분리한다.
- 확인 전에는 좌표를 `null`, 상태를 `pending`으로 유지한다.
- `collection_mountains.verification_point_id`를 올바른 인증지에 연결한다.
- 여러 컬렉션이 같은 산과 같은 인증지를 쓰면 같은 인증지 레코드를 재사용하고, 실제 요구 지점이 다르면 별도로 모델링한다.

### 5) 인증지 좌표와 반경을 하나씩 검증

- 공식/공공/신뢰 가능한 지도 자료로 실제 정상석·봉우리·체크포인트를 교차 확인한다.
- 출처, 확인일, 선택 근거를 `source_note` 또는 별도 관리 데이터에 기록한다.
- 좌표 확인 후에만 `coordinate_status='verified'`로 바꾼다.
- 지형과 GPS 오차를 고려해 지점별 반경을 결정한다. 일괄 근삿값으로 활성화하지 않는다.

### 6) 인증 클라이언트와 서버를 `verification_points` 기준으로 전환

- 인증 시작 시 현재 컬렉션/산에 유효한 `verified` 지점만 선택한다.
- 클라이언트 거리 계산과 DB 서버 재검증이 같은 인증지 좌표·반경을 사용하게 한다.
- `pending`/`retired`/좌표 누락 지점은 명확히 비활성화하고 한국어 안내를 표시한다.
- 공동 인증 참여자도 세션과 동일한 인증지 기준으로 서버에서 재검증한다.
- 기존 인증 세션과의 호환성을 유지하고, 한 세션이 조건을 충족하는 모든 컬렉션에 인정되는 규칙을 테스트한다.

### 7) 클라이언트 임시 이름 매칭을 DB 기반 조회로 교체

- `BAC_NAMES`와 고정된 `BAC_CONNECTED_COUNT`를 최종 데이터 소스로 사용하지 않는다.
- 홈·도감·MY 진행률을 `collections`/`collection_mountains` 멤버십에서 파생한다.
- 대표 컬렉션을 `profiles.primary_collection_id`와 동기화하되 AsyncStorage를 오프라인/초기 로딩용 안전한 폴백으로 유지할지 명시적으로 결정한다.
- 컬렉션 전환이 인증 세션이나 완료 기록을 변경하지 않는지 확인한다.

### 8) 데이터·인증 회귀 검증 후 Phase 6 마무리

- 아래 체크리스트를 자동화 가능한 테스트와 실제 기기/시뮬레이터 수동 테스트로 통과시킨다.
- 그다음 남은 Phase 6 항목인 등장 애니메이션, 햅틱, 마이크로 인터랙션, 전체 접근성 점검을 마무리한다.
- 오래된 README의 캐릭터·마이그레이션·다음 단계 설명을 현재 규칙에 맞게 별도 문서 커밋으로 정리한다.

### 사용자 승인 후속 기능 — 별도 트랙

- 2026-09-11 요청 범위는 명산 도감 리스트/지도, 산 주변 맛집, Apple Fitness 호환 운동 기록, 페이스 기록과 인증자 랭킹이다.
- 상세 규칙과 구현 순서는 `docs/POST_MVP_DISCOVERY_ACTIVITY.md`에 있다.
- 명산 도감의 `리스트 / 지도` 전환은 구현됐다. 지도는 내 위치 중심 50km가 기본이며 10/30/50/100km 반경을 고를 수 있다. 현재 위치는 저장하지 않고, 필터된 현재 컬렉션 가운데 반경 안의 산만 표시한다. 산 좌표는 소수 둘째 자리로 낮춰 정확한 인증 위치를 공개하지 않으며, 좌표 없는 산은 임의로 표시하지 않는다. 기본 핀은 공식 색을 활용한 산 스티커형 마커로 교체했다.
- 다음 독립 작업은 공개 표시/들머리 위치 모델이다. 인증지 좌표를 맛집 검색 중심점으로 재사용하지 않는다.
- Apple Fitness 데이터는 HealthKit 읽기 권한으로 연동하며 Expo Go가 아닌 새 TestFlight 빌드가 필요하다. 원시 경로는 기본적으로 업로드하지 않는다.
- 페이스는 서버에서 거리와 시간으로 계산하고, 확정 인증에 연결된 HealthKit 기록 중 사용자가 공개 동의한 기록만 랭킹에 포함한다. 수동 기록은 개인 기록용이며 랭킹에서 제외한다.

## 9. 검증 체크리스트

### 데이터 무결성

- [x] 산림청 멤버십이 정확히 100개다.
- [x] BAC 멤버십이 정확히 100개다.
- [x] 교집합은 79개, 산림청 전용 21개, BAC 전용 21개다.
- [x] 컬렉션별 `display_order`가 1–100이고 중복·누락이 없다.
- [ ] 동명이산과 서로 다른 대표봉이 올바른 `mountain_id`/`verification_point_id`로 연결된다.
- [ ] 모든 미검증 인증지는 좌표가 없고 `pending`이며 인증에 사용할 수 없다.
- [ ] 모든 `verified` 인증지는 출처와 검증 근거가 있다.

### 진행률과 컬렉션 전환

- [ ] 교집합 산 1건 인증 시 두 컬렉션 진행률이 각각 1 증가한다.
- [ ] 한 컬렉션 전용 산은 해당 컬렉션만 증가한다.
- [ ] 같은 산을 여러 번 인증해도 한 컬렉션에서는 distinct 1개로 센다.
- [ ] 대표 컬렉션을 왕복 전환해도 인증 이력과 공동 인증 멤버가 변하지 않는다.
- [ ] 홈, 명산 도감, MY의 숫자가 같은 멤버십 데이터로 계산된다.
- [ ] 앱 재실행 및 로그인 재접속 후 대표 컬렉션 선택이 기대대로 유지된다.

### GPS 및 공동 인증

- [ ] 인증 화면의 거리와 DB 트리거가 동일한 `verified` 인증지를 사용한다.
- [ ] `pending`, `retired`, 좌표 누락 인증지는 촬영·확정할 수 없다.
- [ ] 인증 반경 밖 세션 생성이 서버에서 거절된다.
- [ ] 초대받은 참여자도 같은 인증지 반경을 독립적으로 통과해야 `confirmed`가 된다.
- [ ] 공동 인증은 한 세션과 여러 멤버로 유지된다.
- [ ] 초대 대상은 맞팔 친구로 제한된다.
- [ ] 공개 화면에 정확한 원시 GPS가 노출되지 않는다.

### 제품·UI 회귀

- [ ] 하단 탭은 정확히 `홈 / 명산 / 인증 / 친구 / MY` 다섯 개다.
- [ ] 산 상세 인증자 목록은 맞팔 그룹이 항상 먼저다.
- [ ] BAC 미완성 데이터는 100/100 사용 가능으로 오해되지 않도록 표시된다.
- [ ] 산과 완료 스탬프가 수집 대상이며 공식 캐릭터는 반복 브랜드 캐스트로만 사용된다.
- [ ] UI 작업은 원본 100PEAKS 콘셉트와 공식 에셋을 실제로 비교했다.
- [ ] 터치 영역 44pt 이상, 대비, 접근성 레이블, 로딩·빈 상태·오류 상태를 확인했다.

### 기술 검증

- [x] 새 DB에서 `0001`부터 최신 마이그레이션과 seed를 순서대로 적용할 수 있다.
- [ ] 마이그레이션은 재실행 안전성 또는 명시된 일회성 동작을 갖는다.
- [x] `npm run typecheck`가 통과한다.
- [x] `npm run lint`가 통과한다.
- [ ] 변경한 Expo Router 경로를 실제로 열어 본다.
- [ ] 게스트 모드와 Supabase 연결 모드를 모두 확인한다.
- [ ] 서비스 역할 키, 개인 토큰, 원시 사용자 위치 같은 비밀·민감 데이터가 커밋되지 않았다.

## 10. 다음 작업자의 시작 보고 형식

코드를 수정하기 전에 `AGENTS.md`에 따라 짧게 보고한다.

```text
Phase/목표:
수정 예정 파일:
충족할 승인 기준:
데이터 출처와 좌표 상태:
```

첫 구현은 위 **8. 다음 구현 우선순위**의 5번부터 시작한다. 이미 완료된 Phase 1–5와 우선순위 1–4를 재작성하거나, 화면만 100개처럼 보이게 만들어 데이터 공백을 숨기지 않는다.
